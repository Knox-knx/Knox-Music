// Local provider entry point — the implementation lives alongside the
// other providers in ../LocalLibraryProvider.ts (kept to avoid breaking
// existing imports). This module gives the requested
// providers/local/provider shape.
export { LocalLibraryProvider } from '../LocalLibraryProvider';
